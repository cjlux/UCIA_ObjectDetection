import treamlit as st
import streamlit as st
dir(st)
from streamlit import stframe
import streamlit as st
dir(st)
help(st.video)
help(st.image)
dir(st)
help(st.dataframe)
dir(st)
help(st.dataframe)
import streamlit as st
help(st.dataframe)
import streamlit as st
help(st.empty)
dir(st)
import bbox_visualiser as bbv
import bbox_visualizer as bbv
dir(bbv)
help(add_label)
help(bbv.add_label)
import bbox_visualizer as bbv
dir(bbv)
help(bbv.add_label)
import bbox_visualizer as bbv
dir(bbv)
help(bbv.add_label)
import PILasOPENCV as ImageFont
font = ImageFont.truetype("arial.ttf", 18)
ft = cv2.freetype.createFreeType2()
import cv2
ft = cv2.freetype.createFreeType2()
cv2.__version__
dir(cv2)
import PILasOPENCV as ImageFont
help(ImageFont.truetype)
font = ImageFont.truetype("ARIAL.ttf", 18)
import PILasOPENCV as ImageFont
font = ImageFont.truetype("/home/ucia/.config/Ultralytics/Arial.ttf", 18)
font.size
font.size()
font.size
font.size.height
import cv2
cv2.getTextSize("aaaa", font, size, thickness)
cv2.getTextSize("aaaa", font, 1, 1)
f'{0.94:3.2f}'
f'{0.94:2.2f}'
f'{0.94:3.3f}'
f'{0.94:.3f}'
f'{0.94:.2f}'
f'{0.94:5.2f}'
data = '1,2,3,4,5,6,7,8,9'
data
class_id, conf, x1, y1, x2, y2, R, G, B = data.split(',')
R
G
B
list(map(lambda x: int((float(x)/255)*32), [R, G, B]))
